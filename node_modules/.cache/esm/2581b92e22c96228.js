let pkg,CustomErrorHandler;_22c‍.x([["default",()=>_22c‍.o]]);_22c‍.w("joi",[["default",["pkg"],function(v){pkg=v}]]);_22c‍.w("../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);

const { ValidationError } = pkg;

const errorHandler = (err, req, res, next) => {
  let statusCode = 404;
  let data = {
    message: err.message,
    ...(process.env.DEBUG_MODE === "true" && { originalError: err.message }),
  };

  if (err instanceof ValidationError) {
    statusCode: 505,
      (data = {
        message: err.message,
      });
  }

  if (err instanceof CustomErrorHandler) {
    statusCode: err.status,
      (data = {
        message: err.message,
      });
  }

  return res.status(statusCode).json(data);
};

_22c‍.d(errorHandler);
